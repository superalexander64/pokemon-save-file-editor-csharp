namespace PokemonSaveFileEditor.Enums
{
    public enum StatusCondition : int
    {
        OK = 0,
        ASLEEP = 4,
        POISONED = 8,
        BURNED = 16,
        FROZEN = 32,
        Paralyzed = 64
    }
}